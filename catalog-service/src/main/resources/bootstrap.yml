spring:
  application:
    name: catalog-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-service
  profiles:
    active: dev
  output:
    ansi:
      enabled: ALWAYS
  config:
    debug: true